image: python:3.5

stages:
  - test
  - release

test:
  stage: test
  script:
    - python setup.py test


release:
  stage: release
  script:
    - pip install wheel
    - python setup.py sdist bdist_wheel
  artifacts:
    paths:
    - dists
  only:
    - master
